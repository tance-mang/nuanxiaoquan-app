# 暖小圈后端依赖包
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pymysql==1.1.0
redis==5.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0
requests==2.31.0
pillow==10.2.0
python-dotenv==1.0.0

# 阿里云OSS（如不上传文件可以删）
oss2==2.18.4

# 数据分析（本地确定性算法用）
pandas==2.1.4
numpy==1.26.3

# 短信服务（注册验证码用，如未启用可删）
tencentcloud-sdk-python==3.0.1074

# 任务队列（如未启用可删）
celery==5.3.4

# 说明：
# 1. 每日推荐暖句由前端直接走一言公开 API（hitokoto.cn），后端不依赖。
# 2. 豆包调用走标准 HTTPS（requests）——火山方舟 v3 兼容 OpenAI 接口格式，不需要专用 SDK。
